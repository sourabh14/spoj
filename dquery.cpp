#include<cstdio>
#define SIZE 300005

long n, q, arr[SIZE], i, j, l, r;

int main() {
	scanf("%ld",&n);
	for (i=0; i<n; i++) scanf("%ld",&arr[i]);
	
	while (q--) {
		scanf("%ld %ld",&l, &r);
	}
	return 0;
}		
